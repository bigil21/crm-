{
  "name": "coastal-crest-crm",
  "version": "1.0.0",
  "private": true,
  "description": "Coastal Crest Roofing CRM for leads, jobs, estimates, and sales team workflow.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check login.js && node --check logout.js"
  },
  "engines": {
    "node": ">=20"
  }
}
