{
  "name": "jobcrest-crm",
  "version": "1.0.0",
  "private": true,
  "description": "JobCrest CRM for contractor leads, jobs, estimates, documents, and sales workflows.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check login.js && node --check logout.js && node --check project-conversations-v67.js && node --check workflow-checklists-v65.js && node --check sw.js",
    "check:go-live": "npm run check && node go-live-check.js"
  },
  "engines": {
    "node": ">=20"
  }
}
