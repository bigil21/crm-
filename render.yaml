services:
  - type: web
    name: crm
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /api/health
    # Response caching/security headers are controlled by the Node server.
